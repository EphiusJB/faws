import ReactLoading from 'react-loading'

export default function Loading() {
    return(
        <div>
            <ReactLoading type='cyclon' color="#078f00" height={100} width={50}/>
        </div>
    );
}